=== KIK-Red Modifications ===
Build Date: 2026-06-09 (Updated)

[PACKAGE RENAME]
- Renamed package from kik.blue to kik.red across all smali, xml, resources
- Updated AndroidManifest.xml package, activities, services, providers, receivers, permissions
- Renamed all smali directory structures from kik/blue/ to kik/red/
- Updated strings.xml: app_name = "KIK-Red", all "Blue Kik" -> "KIK-Red"
- Updated apktool.yml apkFileName to KIK_Red
- Updated StaticConstants.smali: KIK_APP_NAME = "KIK-Red", KIK_PACKAGE_NAME = "kik.red"
- Bulk replaced kik.blue, kik.android, kik/blue, Blue Kik, Blue_Kik references (5123 files)

[CRASH FIX]
- Removed duplicate R class files from smali_classes14/kik/android/ (R$id, R$color, R$string, R$style)
  These were Compose UI inner classes conflicting with the real Kik R class, causing NoSuchFieldError at runtime
- Fixed apktool.yml version from fake 2.12.0 to real 2.11.0

[ISLAND/WORK PROFILE SUPPORT]
- Added android:crossProfile="true" to application tag in AndroidManifest.xml
- Added cross-profile package queries for parallel install on work + personal profiles

[UNCROSSKABLE]
- GuestContentStatus$ActiveBroadcastEnded.smali: d() method now always returns false (prevents kick detection)
- LiveFeedViewModel.smali: A1() method - flipped if-eqz to goto to skip kick/block API calls
- Host/bouncer kick initiation now always falls through to "not kicked" path
- User can never be kicked from a live stream

[UNLIMITED VIDEO]
- I11111lII11Il1lI.smali: llllIl11l1lIIl11() returns 0x7fffffff (max int) instead of 0x3a980 (240s)
- I11111lII11Il1lI.smali: Il1IlllI1ll1II11() sets maxDuration to 0x7fffffff instead of 0x3a980
- No more 4-minute recording limit, no time restrictions whatsoever

[LIKE SYSTEM]
- LiveBroadcastActivityModel.smali: likesLimit changed from 0x1e (30) to 0x30d4 (12500)
- LiveBroadcastActivityModel.smali: likesDelayMs changed from 0xbb8 (3000ms) to 0x64 (100ms) - near-instant batching
- w3.smali: Removed p8(I) call in j8() method - hides like count display (overlay text hidden)
- sendLikesClickedNoUI() now loops 5000 times (0x1388) - auto-sends 50000 likes in one tap
- Like overlay status text no longer visible during likes

[LIVE AUTO-ENABLE]
- LiveViewModel$isLiveEnabled$2.smali: Replaced full method to always return TRUE observable
- No remote config check, no age check, no token check - live always enabled for all users

[FAKE DEVICE ID]
- bb/a.smali getAndroidId(): Now generates fake ID using "kikred" prefix + nanoTime instead of reading real GSF Android ID
- Prevents device-level bans on Kik network - each account gets unique fake device ID

[AUTO DOB]
- KikRegistrationFragmentAbstract.smali: Calendar G4 auto-initialized to 16/12/1982 (0x7be year, month 11, day 16)
- No rate limiting on registration - unlimited accounts with no cooldown between creation

[LOGO/ICONS RECOLORED]
- All blue tones converted to red in: bluekik_icon, bluekik_icon_small, blue_splash, flat_kik_logo, img_navbar_logo, img_splash
- All density variants (hdpi/xhdpi/xxhdpi/xxxhdpi) recolored
- Launcher icons (mipmap-*/ic_launcher, ic_foreground, ic_background) recolored

[ABOUT SECTION]
- blue_prefs_update.xml: About summary changed to "Made by jnetai.com"
- preferences_blue.xml: Developer section updated to "KIK-Red - Made by jnetai.com" with feature list
- Download Update link updated to point to GitHub releases
- Added "Share KIK-Red" button

[GITHUB ACTIONS]
- Created .github/workflows/build.yml
- Auto-builds on push to main/master
- Installs Android SDK command-line tools and apktool 2.11.0
- Signs APK with ciphervault keystore from GitHub Secrets
- Creates GitHub Release automatically with zipaligned APK
- Uses ubuntu-22.04 runner for aapt1 compatibility
- Same keystore used on every build for seamless updates
